<Window x:Class="OOP_NW_Airbnb.AdminManage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OOP_NW_Airbnb"
        mc:Ignorable="d"
        Title="AdminManage" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add" Width="100" Margin="5" Click="Add_Click"/>
            <Button Content="Edit" Width="100" Margin="5" Click="Edit_Click"/>
            <Button Content="Delete" Width="100" Margin="5" Click="Delete_Click"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid x:Name="QuestionsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Question" Binding="{Binding Text}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
